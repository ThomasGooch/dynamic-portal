{
  "name": "@portal/public-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The brokered external surface — versioned apart from the catalog on purpose",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --build --force"
  },
  "dependencies": {
    "@portal/identity": "workspace:*",
    "@portal/mcp-gateway": "workspace:*",
    "@portal/protocol": "workspace:*",
    "@portal/registry": "workspace:*"
  }
}

{
  "name": "@portal/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Which satellites exist, who may see them, and what to do when one is unwell",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --build --force"
  },
  "dependencies": {
    "@portal/catalog": "workspace:*",
    "@portal/identity": "workspace:*",
    "@portal/protocol": "workspace:*",
    "yaml": "^2.6.0",
    "zod": "^3.24.0"
  }
}

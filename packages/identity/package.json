{
  "name": "@portal/identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Principal, tenancy, audience and the audit record — the part you cannot refactor later",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --build --force"
  },
  "dependencies": {
    "@portal/protocol": "workspace:*",
    "zod": "^3.24.0"
  }
}

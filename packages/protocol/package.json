{
  "name": "@portal/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Portal UI Protocol (PUP) — the wire contract between satellites and the hub",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --build --force"
  },
  "dependencies": {
    "zod": "^3.24.0"
  }
}
